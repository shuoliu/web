form(name='jsonmaker')
	.form-group
		label Put field names for JSON file, delimited by SPACE:
		input.form-control(ng-model='table.cols', placeholder='f1  f2  f3', required)
	.form-group
		label Delimiter for values by default is SPACE. Otherwise input a different one:
		input.form-control(ng-model='table.delimiter')
	.form-group
		label Put contents by rows, use delimiter to split values:
		textarea.form-control(ng-model='table.rows',rows='6',placeholder="v1  v2  v3", required)
	p.help-block JSON Maker interpreters row-based data into JSON style object array.
	p.help-block Eg. Field names: r1 r2, rows: a b; c; d e f
	p.help-block &rarr; [{"r1":"a","r2":"b"},{"r1":"c"},{"r1":"d","r2":"e","r2":"f"}]
	button.btn.btn-primary(ng-click='makejson()',ng-disabled="jsonmaker.$invalid") Make it!
	
br
.from-group.well
	p {{json_output}}